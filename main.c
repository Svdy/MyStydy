
#include <stdio.h>
#include "askname.h"

int main(int argc, char **argv) {
    char firstName[255], lastName[255];
    askname(first, last);
    printf("Hello, %s %s!\n", firstName, lastName);
    return 0;
}
